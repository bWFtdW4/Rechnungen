package rechnungen;

import javax.swing.JOptionPane;

public class Eingabe {
    String eingabe;
    double laenge = 0;
    double breite = 0;

/*
    double eingabeLaenge (double laenge){
        int i = 1;
        double tempLaenge;
        while(i == 1){
            eingabe = JOptionPane.showInputDialog("Länge Eingabe: ");
            tempLaenge = Double.parseDouble(eingabe);
            if(tempLaenge == 0){
                System.out.println("darf nicht 0 sein");
            }else{
                this.laenge = tempLaenge;
                System.out.println();
            }
        }return (this.laenge);
    }
    
    double eingabeBreite (double breite){
        int i = 1;
        double tempBreite;
        while(i == 1){
            eingabe = JOptionPane.showInputDialog("Länge Eingabe: ");
            tempBreite = Double.parseDouble(eingabe);
            if(tempBreite == 0){
                System.out.println("darf nicht 0 sein");
            }else{
                this.breite = tempBreite;
            }
        }return (this.breite);
    }
*/    
}
